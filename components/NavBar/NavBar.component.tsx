import React from 'react';
import styled from 'styled-components';

const StyledNavbar = styled.div`
    width: 100%;
    height: 10px;
    background-color: black;
    color: white;
`;

export const NavBar = () => <StyledNavbar>NavBar</StyledNavbar>;
